import styled from "styled-components";

const Home = (props) => {
    return (
        <Component>
            <h2>.</h2>
        </Component>
    )
};

const Component = styled.div``;

export default Home;