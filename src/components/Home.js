import styled from "styled-components";

const Home = (props) => {
    return (
        <Component>
            <h2>Hello</h2>
        </Component>
    )
};

const Component = styled.div``;

export default Home;